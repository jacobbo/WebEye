WebEye is a collection of .NET controls. 

1. WebCameraControl is a DirectShow-based web camera control. 
2. StreamPlayerControl is a FFmpeg-based stream player control. 

The controls are simple and easy to use, have no additional dependencies and a minimalistic interface.